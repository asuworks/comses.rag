meta {
  name: chat
  type: http
  seq: 2
}

post {
  url: http://{{server_ip}}:8000/chat?question=what's on the menu?
  body: none
  auth: bearer
}

params:query {
  question: what's on the menu?
}

auth:bearer {
  token: 
}
